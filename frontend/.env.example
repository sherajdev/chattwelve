# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# BetterAuth Configuration
# PostgreSQL connection string for BetterAuth (self-hosted Supabase)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@192.168.50.253:5432/postgres

# BetterAuth secret - generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-random-32-char-secret-here

# BetterAuth URL (your app's URL)
BETTER_AUTH_URL=http://localhost:3000

# Public app URL for client-side redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000
